import { TStatus, TPriority } from "./types/types";

const statuses: TStatus[] = ["Backlog", "InProgress", "Done"];
const priorities: TPriority[] = ["Low", "Medium", "High"];

export { statuses, priorities };
